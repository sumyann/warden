title = "warden gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "examples/vulnerable-demo is intentionally full of fake secrets used to demonstrate Warden's own detection rules -- see examples/vulnerable-demo/README.md."
paths = [
  '''examples/vulnerable-demo/.*''',
]
