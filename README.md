# Warden

Static security scanner for MCP (Model Context Protocol) servers.

> Placeholder -- full README (pitch, quickstart, rule table, benchmark
> summary, GitHub Action usage, OSS/hosted boundary) is written later in
> Phase 2 of the OSS split. This stub exists only so `pip install -e .`
> has a readme file to satisfy the package metadata during development.
